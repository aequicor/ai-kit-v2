# IntelliJ IDEA
.idea/
*.iml
*.iws
*.ipr
out/

# Gradle
.gradle/
build/
gradle-app.setting
!gradle-wrapper.jar
!gradle-wrapper.properties

# Kotlin
*.class
*.kotlin_module

# Claude Code
.claude/settings.local.json
.claude/launch.json
.claude/worktrees/

# OpenCode
.opencode/

# AI-Kit per-project setup
# The kit-setup binary is platform-specific and downloaded fresh per project;
# never check it in. Generated kit artifacts inside test fixtures
# (sample-* projects) follow the same rule. Plan files authored by /kit
# during dogfooding are NOT ignored — Session 2 of the v3 pipeline locates
# them via `git log --grep="kit: plan for"`, so they must live in history.
.aikit/bin/

# OS
.DS_Store
Thumbs.db
